<% if @netdev_mac_list %>
<% @netdev_mac_list.each_with_index do |d,i| -%>
SUBSYSTEM=="net", ACTION=="add", ATTR{address}=="<%= d %>", NAME="phone<%= i %>", RUN+="<%= @bin_dir %>/<%= @masq_script %> <%= @lan_iface %> phone<%= i %>"
<% end %>
<% end %>
SUBSYSTEM=="net", ACTION=="add", ATTR{address}=="<%= @wifi_mac %>", NAME="wifi0", RUN+="/usr/bin/service hostap restart"
